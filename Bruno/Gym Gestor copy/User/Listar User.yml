info:
  name: Listar User
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/api/users"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
