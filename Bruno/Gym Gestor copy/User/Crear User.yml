info:
  name: Crear User
  type: http
  seq: 3

http:
  method: POST
  url: "{{apiUrl}}/api/users"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
